= collection_select @product, :middle_category, Category.where(division: division).order('sort_by'), :id, :name, {prompt: '---'}, {class: 'sell-form__select', id: 'middle_category'}
= fa_icon('angle-down')
.sell-form__alert#alert-middle-category
