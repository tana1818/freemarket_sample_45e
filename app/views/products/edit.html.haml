= render partial: 'sell-header'
%main.sell-main
  %h2.sell-main__title
    商品の情報を入力
  = form_for @product, html: {class: 'dropzone', id: 'my-awesome-dropzone'}, url: product_images_path(@product), method: :post do |f|
    = render partial: 'sell-form', locals: {f: f}
= render partial: 'sell-footer'
